# Runtime requirements.
#
# Every one of these ships in Kaggle's default Python image. Do not pip install anything
# inside a GPU notebook: it costs minutes of a 30 GPU-h/week quota and can break the CUDA
# build of torch. This file exists so the repo also runs locally, where CPU is enough for
# everything except the training script.
#
#   python -m pip install -r requirements.txt
#
# For a CPU-only local install of torch:
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cpu

numpy>=1.24
scipy>=1.10
torch>=2.0
soundfile>=0.12
scikit-learn>=1.3
matplotlib>=3.7
PyYAML>=6.0

# Note what is NOT here. The audit and the baselines -- milestone 1, the first reportable
# number this project produces -- import numpy, scipy and scikit-learn only. No torch. That
# is deliberate: the leakage audit and the bar the neural counter must beat run on a CPU
# notebook, at zero GPU cost, before any training is attempted. See docs/DIAGNOSIS.md.
#
# Also deliberately absent, as in the sibling repo: asteroid, librosa, torchaudio,
# speechbrain. They are heavy, they pin torch versions, and everything needed from them
# (STFT, SI-SDR, spectral features) is a dozen lines of scipy.
